# Snippet de nginx por proyecto. Se incluye dentro del server block del blue-green.
# Añade aquí locations o directivas específicas (estáticos, streaming, etc.).

# Ejemplo: servir archivos estáticos desde la app (ajusta alias a la ruta real en tu imagen).
# location /static/ {
#     alias /app/public/;
#     expires 1d;
#     add_header Cache-Control "public, immutable";
# }

# Ejemplo: streaming (desactivar buffering y timeouts largos).
# location /stream/ {
#     proxy_pass http://app_active;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_buffering off;
#     proxy_read_timeout 3600s;
#     proxy_send_timeout 3600s;
# }

# Si no necesitas nada extra, deja este archivo vacío o solo con comentarios.
