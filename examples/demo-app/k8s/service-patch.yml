apiVersion: v1
kind: Service
metadata:
  name: rx-mi-aplicacion-service
  annotations:
    app.k8s/managed: "true"
spec:
  type: NodePort
  selector:
    app: rx-mi-aplicacion
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
